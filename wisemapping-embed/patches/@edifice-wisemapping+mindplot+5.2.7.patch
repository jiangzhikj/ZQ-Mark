diff --git a/node_modules/@edifice-wisemapping/mindplot/src/components/SvgImageIcon.ts b/node_modules/@edifice-wisemapping/mindplot/src/components/SvgImageIcon.ts
index aa34d83..a585965 100644
--- a/node_modules/@edifice-wisemapping/mindplot/src/components/SvgImageIcon.ts
+++ b/node_modules/@edifice-wisemapping/mindplot/src/components/SvgImageIcon.ts
@@ -24,18 +24,18 @@ import iconFamily from './model/SvgIconFamily.json';
 import Topic from './Topic';
 import SvgIconModel from './model/SvgIconModel';
 
-function importAll(r) {
-  const images = {};
-  r.keys().forEach((item) => {
-    images[item.replace('./', '')] = r(item);
-  });
-  return images;
+/** Webpack require.context 在 Vite 中不可用；与 importAll(require.context(...)) 等价（键为文件名） */
+const __iconModules = import.meta.glob('../../assets/icons/*.{png,svg}', {
+  eager: true,
+  import: 'default',
+}) as Record<string, string>
+const images: Record<string, string> = {}
+for (const __path of Object.keys(__iconModules)) {
+  const __slash = __path.lastIndexOf('/')
+  const __name = __slash === -1 ? __path : __path.slice(__slash + 1)
+  images[__name] = __iconModules[__path]
 }
 
-// eslint-disable-next-line @typescript-eslint/ban-ts-comment
-// @ts-ignore
-const images = importAll(require.context('../../assets/icons', false, /\.(png|svg)$/));
-
 class SvgImageIcon extends ImageIcon {
   private _topicId: number;
 
